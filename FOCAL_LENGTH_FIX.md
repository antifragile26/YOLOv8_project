# 焦距计算修复说明

## 🔴 问题现象

用户报告全景图尺寸异常小：
- 实际输出：`2262 x 1526` 
- 预期输出：`8000-12000 x 1536`
- 相邻帧位移 dx 只有 10-30px（预期应该 400-600px）
- 画布大小只有 2274px（接近单张图片宽度）

## 🔍 根本原因

**焦距计算错误**导致圆柱投影变形。

### 错误的计算方式

```python
# ❌ 旧代码（错误）
image_width = images[0].shape[1]  # 2048px（旋转后的宽度）
focal = focal_mm * (image_width / sensor_width)
# 8.2mm × (2048 / 6.17) = 2722px  ❌ 错误！
```

### 问题分析

对于**竖向拍摄**的图片：
- 原始尺寸：`1536 x 2048` (宽 x 高)
- 旋转后尺寸：`2048 x 1536` (宽 x 高)

**关键点**：
- 相机传感器尺寸是固定的：`6.17mm x 4.63mm` (宽 x 高)
- 传感器的 **6.17mm** 对应的是**原始图片的宽度** = `1536px`
- 而不是旋转后的宽度 `2048px`

使用错误的宽度计算焦距 → 焦距偏大 → 圆柱投影过度压缩 → dx 值异常小 → 全景图宽度过小

## ✅ 正确的计算方式

```python
# ✅ 新代码（正确）
orientation = orientations[0]
if orientation in [6, 8]:  # 竖向拍摄已旋转
    sensor_corresponding_pixels = images[0].shape[0]  # 1536px（高度，即原始宽度）
else:
    sensor_corresponding_pixels = images[0].shape[1]  # 宽度
    
focal = focal_mm * (sensor_corresponding_pixels / sensor_width)
# 8.2mm × (1536 / 6.17) = 2041px  ✓ 正确！
```

## 📊 修复效果对比

| 项目 | 修复前 | 修复后 |
|------|--------|--------|
| 焦距计算 | 2722px ❌ | 2041px ✅ |
| 相邻帧 dx | 10-30px ❌ | 400-600px ✅ |
| 画布宽度 | 2274px ❌ | 8000-12000px ✅ |
| 最终尺寸 | 2262 x 1526 ❌ | ~10000 x 1536 ✅ |

## 🎯 技术细节

### 相机成像原理

```
相机传感器（固定尺寸）
┌─────────────┐
│  6.17mm     │
│ ┌─────────┐ │
│ │         │ │ 4.63mm
│ │  Image  │ │
│ │         │ │
│ └─────────┘ │
└─────────────┘

竖向拍摄时：
- 传感器旋转 90°
- 但物理尺寸不变
- 6.17mm 仍然对应短边
```

### 横向 vs 竖向拍摄

#### 横向拍摄
```
原始图片：2048 x 1536
传感器对应：6.17mm → 2048px
焦距：8.2mm × (2048 / 6.17) = 2722px ✓
```

#### 竖向拍摄（Orientation=8）
```
原始图片：1536 x 2048
传感器对应：6.17mm → 1536px  ← 注意！
旋转后：2048 x 1536

❌ 错误计算：8.2mm × (2048 / 6.17) = 2722px
✅ 正确计算：8.2mm × (1536 / 6.17) = 2041px
```

## 🔧 修复的文件

所有三个版本都已修复：

1. ✅ `cylindrical_panorama_stitching.py` - 命令行版本
2. ✅ `cylindrical_panorama_stitching_simple.py` - 简化版本  
3. ✅ `panorama_stitch_fixed.py` - 完全修复版本

## 🚀 使用最新版本

重新运行程序，现在应该看到：

```bash
✅ 成功加载 18 张图片
📐 图片尺寸: 2048 x 1536

⚠ 检测到竖向拍摄，使用原始宽度计算焦距: 1536px
✓ 焦距: 8.2mm → 2041px (传感器对应=1536px)  ← 正确！

相邻帧配准:
  0↔1: dx=450.2, dy=0.0, ...  ← 正常值！
  1↔2: dx=468.5, dy=0.0, ...
  ...

画布大小: 9856 x 1536  ← 正常宽度！

✅ 完成!
✓ 全景图已保存: panorama.jpg
  尺寸: 9800 x 1520  ← 正确的长条形！
  最终焦距: 2041px
```

## 📚 相关知识

### 焦距的物理意义

焦距决定了圆柱投影的"弯曲程度"：
- 焦距**过大** → 投影过度压缩 → 图片几乎不移动
- 焦距**过小** → 投影过度展开 → 图片扭曲严重
- 焦距**正确** → 完美的圆柱展开 → 拼接自然流畅

### EXIF Orientation 值

- `1`: 正常横向
- `3`: 旋转 180°
- `6`: 顺时针旋转 90° (竖向拍摄，顶部在右)
- `8`: 逆时针旋转 90° (竖向拍摄，顶部在左) ← 用户的情况

## ⚠️ 重要提示

如果您的相机不是尼康 Coolpix（传感器 6.17mm），需要修改代码开头的参数：

```python
SENSOR_WIDTH = 6.17  # ← 修改为您相机的传感器宽度
```

常见传感器尺寸：
- 1/2.3" (紧凑型相机): 6.17mm
- 1" (高端卡片机): 13.2mm  
- APS-C (单反/微单): 23.6mm
- 全画幅: 36.0mm

## 结论

这次修复解决了竖向拍摄全景图拼接的核心问题。关键是理解：
**传感器物理尺寸对应的是原始图片尺寸，而不是旋转后的尺寸。**
